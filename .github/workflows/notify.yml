name: Free Games Notifier

on:
  schedule:
    - cron: '0 16 * * *'  # ทุกวัน 23:00 น. เวลาไทย
  workflow_dispatch:        # กดรันมือได้จาก GitHub

permissions:
  contents: write

jobs:
  notify: 
    runs-on: ubuntu-latest
    concurrency:
      group: free-games-notifier
      cancel-in-progress: true
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          persist-credentials: true

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install requests

      - name: Validate Discord webhook secret
        env:
          DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}
        shell: bash
        run: |
          if [ -z "$DISCORD_WEBHOOK_URL" ]; then
            echo "::error::DISCORD_WEBHOOK_URL secret is empty or missing"
            exit 1
          fi
          if [[ "$DISCORD_WEBHOOK_URL" != https://discord.com/api/webhooks/* ]]; then
            echo "::warning::DISCORD_WEBHOOK_URL does not look like a Discord webhook URL"
          fi

      - name: Run notifier
        env:
          DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}
          RUN_ONCE: '1'
          DEBUG: '1'
        run: python notify.py

      - name: Commit notified/status files if changed
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add notified.json
          git commit -m "Update notified/status files" || echo "No changes to commit"
          git push origin HEAD:main
        shell: bash
